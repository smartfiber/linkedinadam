import { Link } from "react-router";
export default function DevelopmentConsole() {
  return <main className="workspace-page"><header className="workspace-header"><div><p className="eyebrow">DEVELOPMENT / COMING SOON</p><h1>AI Development Console</h1><p>Architecture preview only. No model, repository write, shell, job, or PR endpoint is connected.</p></div><Link className="secondary-link" to="/development">Development</Link></header><section className="console-shell" aria-disabled="true"><div className="console-banner"><span className="development-status attention">Not Connected</span><strong>Execution is disabled</strong></div><fieldset disabled><div className="console-grid"><label>Repository<select><option>colossalbreacker/net-x</option></select></label><label>Work item<input placeholder="Select a Development Request" /></label><label>Base branch<select><option>Not connected</option></select></label><label>Model<select><option>No models connected</option></select></label><label>Agent / role<select><option>No execution agents configured</option></select></label><label>Mode<select>{["Analyze","Implement","Review","Test","Prepare PR"].map(value => <option key={value}>{value}</option>)}</select></label></div><label>Prompt composer<textarea rows={6} placeholder="Future approval-controlled instructions" /></label><button>Start job</button></fieldset><div className="console-output-grid">{["Plan","Files inspected","Diff","Tests","Findings","Approval"].map(title => <section key={title}><h2>{title}</h2><p>Waiting for a future approved integration.</p></section>)}</div></section></main>;
}
